<Window x:Class="HttpPing.AddHostDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加主机" Width="420" Height="200"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E" Foreground="White"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="网址 (URL):" Foreground="#CCC" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="UrlBox" FontSize="14" Padding="6"
                 Background="#333" Foreground="White" BorderBrush="#555"
                 Text="http://172.16.2.203"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="别名 (可选):" Foreground="#CCC" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="AliasBox" FontSize="12" Padding="4" Width="240"
                     Background="#333" Foreground="White" BorderBrush="#555"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Width="70" Margin="0,0,8,0" Click="Ok_Click"
                    Background="#4CAF50"/>
            <Button Content="取消" Width="70" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
