<Window x:Class="HttpPing.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HttpPing"
        UseLayoutRounding="True"
        Height="95"
        MinWidth="500"
        SizeToContent="Width"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        Background="#282829"
        BorderBrush="#636363"
        BorderThickness="1"
        Topmost="True"
        ShowActivated="False"
        AllowsTransparency="True"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        SizeChanged="Window_SizeChanged"
        RenderTransformOrigin="1,1">
    <Window.RenderTransform>
        <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1"/>
    </Window.RenderTransform>
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Duration="00:00:00.3" Storyboard.TargetProperty="Opacity" From="0" To="0.99" />
                    <DoubleAnimation Duration="00:00:00.2" Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleX" From="0" To="1" />
                    <DoubleAnimation Duration="00:00:00.2" Storyboard.TargetName="scaleTransform" Storyboard.TargetProperty="ScaleY" From="0" To="1" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Close button (top-right, shows on hover) -->
        <Grid HorizontalAlignment="Right">
            <Button Padding="6" Margin="0,1,3,0" Background="Transparent"
                    HorizontalAlignment="Right" IsTabStop="False"
                    ToolTip="关闭" Click="Close_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Name="btnBorder" BorderThickness="1" BorderBrush="Transparent"
                                CornerRadius="4" Opacity="0.5" Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}">
                            <TextBlock Text="✕" FontSize="12" Foreground="White"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#000"/>
                                <Setter TargetName="btnBorder" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="BorderBrush" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Visibility" Value="Hidden"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=IsMouseOver}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- Status change list -->
        <ListBox Name="StatusList"
                 Grid.Row="1"
                 Background="Transparent"
                 SelectionMode="Single"
                 BorderThickness="0"
                 IsTabStop="False"
                 MaxWidth="1200"
                 FocusVisualStyle="{x:Null}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 VerticalAlignment="Top"
                 MouseLeftButtonUp="Window_MouseLeftButtonUp">
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <DockPanel LastChildFill="True" HorizontalAlignment="Stretch">
                        <!-- Status glyph -->
                        <TextBlock DockPanel.Dock="Left"
                                   Text="{Binding Glyph}"
                                   FontSize="18"
                                   VerticalAlignment="Center"
                                   Padding="5,0,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsUp}" Value="True">
                                            <Setter Property="Foreground" Value="#859900"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsUp}" Value="False">
                                            <Setter Property="Foreground" Value="#dc322f"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <!-- Timestamp -->
                        <TextBlock DockPanel.Dock="Left"
                                   Text="{Binding TimestampText}"
                                   FontSize="15"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Opacity="0.6"
                                   Padding="6,0,0,0"/>
                        <!-- Hostname -->
                        <TextBlock DockPanel.Dock="Left"
                                   Text="{Binding DisplayName}"
                                   FontSize="17"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Padding="9,0,0,0"/>
                        <!-- Arrow -->
                        <TextBlock DockPanel.Dock="Left"
                                   Text="&#x27A1;"
                                   FontSize="17"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Opacity="0.6"
                                   Padding="5,0"/>
                        <!-- Status text -->
                        <TextBlock DockPanel.Dock="Left"
                                   Text="{Binding StatusText}"
                                   FontSize="17"
                                   VerticalAlignment="Center"
                                   Foreground="{Binding StatusColor}"/>
                    </DockPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
